import { LoaderIcon } from "./style";
import { BiLoader } from "react-icons/bi";
const Loader = () => {
  return (
    <LoaderIcon>
      <BiLoader size={120} />
    </LoaderIcon>
  );
};

export default Loader;
