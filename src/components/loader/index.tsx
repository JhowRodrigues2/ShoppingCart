import { LoaderIcon } from "./style";
import { BiLoader } from "react-icons/bi";
const Loader = () => {
  return (
    <LoaderIcon>
      <BiLoader size={103} />
    </LoaderIcon>
  );
};

export default Loader;
